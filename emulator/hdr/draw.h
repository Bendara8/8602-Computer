#ifndef HDR_DRAW
#define HDR_DRAW

void drawRegisters(
	int line
);

void drawCode(
	int line
);

void drawStack(
	int line
);

void drawMemory(
	int line
);

void drawFlash(
	int line
);

void drawConsole(
	int line
);

void drawConsoleLine(
	void
);

#endif